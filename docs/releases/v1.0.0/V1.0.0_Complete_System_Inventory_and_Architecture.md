# V1.0.0 - Complete System Inventory & Architecture

**Release:** V1.0.0  
**Baseline finalized:** September 2026  
**Scope:** Canonical vacation-rental property application plus clearly separated master/platform infrastructure  

> This document is the permanent V1.0.0 system baseline. It records implemented capabilities, architecture, canonical fresh-client deployment contents, known limitations, and the items that could not be verified live during the audit.


---

## 1. Executive Summary

This is a **single-property vacation rental management application** branded as "Tiki Cottage." It provides a complete guest-facing booking website and a full-featured administrator dashboard for managing bookings, payments, calendar, pricing, content, emails, cleaning, and maintenance.

- **Primary purpose**: End-to-end vacation rental management - guest booking, payment, calendar sync, automated emails, and property operations.
- **Public guest experience**: A polished listing page with photo gallery, photo tour, amenities, host info, neighborhood highlights, reviews, FAQs, pricing, availability, and booking/inquiry flows.
- **Administrator experience**: A comprehensive dashboard covering calendar, bookings, cleaning, maintenance, property content, pricing, fees, payments, email automation, calendar sync, photos, and account/branding settings.
- **Major external integrations**: Stripe (test and live), SMTP/Resend email, external iCal calendar platforms (Airbnb/VRBO/Booking.com), and a SaaS platform management layer (Netlify/Bolt Database provisioning for multi-client deployments).
- **Overall architecture**: React SPA frontend talking directly to Bolt Database (Postgres + Auth + Storage), with server-side logic in Bolt Database Edge Functions (Deno) and scheduled pg_cron jobs. No traditional backend server is required for the property application itself (a legacy Express/Prisma backend exists in source but is not part of the active deployment).

---

## 2. Technology Stack

- **Frontend framework**: React 18 (SPA)
- **Language**: TypeScript
- **Build tooling**: Vite 5
- **Styling/UI libraries**: Tailwind CSS 3, lucide-react (icons)
- **Routing**: React Router 6
- **Database/backend**: Bolt Database (PostgreSQL 15 + PostgREST)
- **Authentication**: Bolt Database Auth (email/password, JWT sessions)
- **Serverless/Edge Functions**: Bolt Database Edge Functions (Deno runtime, `jsr:@supabase/functions-js`)
- **Storage**: Bolt Database Storage (buckets: `branding`, `property-photos`)
- **Payment provider**: Stripe (Checkout Sessions, webhooks, refunds) - supports separate test and live credentials
- **Email provider architecture**: Pluggable - SMTP (via `npm:nodemailer@6`) or Resend API, configured per-property in database with secrets in Bolt Database Vault
- **Hosting/deployment**: Netlify (frontend SPA with `/* â†’ /index.html` redirect), Bolt Database (database, auth, edge functions, storage)
- **Major third-party packages**: `@supabase/Bolt Database-js@2`, `@stripe/stripe-js`, `nodemailer@6` (edge function import), `react-router-dom@6`

---

## 3. Application Architecture

```
Guest Browser (React SPA)
  â”œâ”€ Direct Bolt Database queries (public_availability view, properties, content tables)
  â”œâ”€ Edge Function calls (booking creation, checkout, booking lookup)
  â””â”€ Stripe.js redirect (checkout session)
         â”‚
         â–¼
Bolt Database (Postgres + Auth + Storage + Edge Functions)
  â”œâ”€ RLS policies (public read for content, authenticated+is_admin_user for admin)
  â”œâ”€ Edge Functions (Deno): booking, payment, email, iCal, admin actions
  â”œâ”€ pg_cron scheduled jobs: payment cleanup, archiving, inquiry cleanup, email scheduler
  â””â”€ Vault: encrypted secret storage for Stripe/email credentials
         â”‚
         â–¼
External Services
  â”œâ”€ Stripe API (checkout sessions, refunds)
  â”œâ”€ SMTP server or Resend API (email delivery)
  â”œâ”€ External iCal feeds (Airbnb/VRBO/Booking.com import)
  â””â”€ Platform SaaS layer (Netlify API, Bolt Database Management API for client provisioning)

```

---

## 4. Public / Guest Website Features

- **Property listing page** (`/`): Hero photo grid, title, rating, location, highlights, description, host section, amenities, sleeping arrangements, location map, neighborhood highlights, local recommendations, reviews, FAQs, Things to Know, contact/inquiry section
- **Photo gallery**: The homepage uses `PropertyHero`, a 5-photo grid (1 large + 4 small) with a "Show all photos" button linking to the Photo Tour. Clicking any photo opens the shared `Lightbox` component. The standalone `PhotoGallery.tsx` component exists in source but is **not imported or used** by any active page.
- **Photo Tour** (`/photos`): Dedicated page with tabbed sections (exterior, interior, rooms), IntersectionObserver-based tab highlighting, hash deep-linking to specific sections, shared `Lightbox`, feature/amenity callouts per section
- **Where You'll Sleep**: Sleeping arrangements synced with Photo Tour - lookup keyed by `sec.slug` (not `sec.id`), matching generated slugs like `section-bedroom-1`. Falls back to `sleeping_arrangements.room_name` and `sleeping_arrangements.image_url` when no matching Photo Tour section is found.
- **Amenities**: Categorized amenity list with icons, toggle-controlled visibility
- **Host information**: Host name, photo, years hosting, response rate, bio
- **Location/map**: Property address, neighborhood highlights with subtitles
- **Local recommendations**: Curated nearby attractions/restaurants
- **Guest information**: Guest info section with adults/children/infants/pets breakdown
- **Things to Know**: House rules, policies, check-in/check-out times, pet policy, accessibility notes, cancellation policy - with homepage visibility toggle and custom heading
- **FAQs**: Q&A section with visibility controls
- **Contact/inquiry**: Contact form that creates an inquiry record and triggers email notifications
- **Booking card**: Sticky booking widget with date range picker, guest count selector (adults/children/infants/pets), real-time pricing quote, minimum-night enforcement, availability checking
- **Pricing display**: Nightly rate with 4-level precedence, fees breakdown, taxes, total
- **Booking request flow** (manual mode): Guest submits request â†’ `pending_review` status â†’ admin approves â†’ confirmed
- **Booking checkout flow** (Stripe mode): Guest selects dates â†’ Stripe Checkout redirect â†’ `pending_payment` â†’ webhook confirms â†’ `confirmed`
- **Branding**: Custom logo, favicon, SEO title, meta description, homepage taglines, badges - all applied dynamically from `account_settings`
- **Responsive/mobile**: Tailwind responsive classes throughout, mobile-friendly navigation

---

## 5. Administrator Dashboard

**Navigation structure** (via `AdminDashboard.tsx`):

- **Overview** (`/admin`): System status dashboard showing payment mode, email provider, calendar sources, recent bookings, quick stats
- **Calendar** (`/admin/calendar`): Monthly calendar view showing bookings (by status), owner blocks, iCal imports, cleaning tasks; detail popover for each event; links to booking detail; includes Availability Editor and Calendar Sync tabs
  - **Availability Editor**: Owner blocks (date ranges with reason/block type), date availability overrides (block specific dates, set custom min-nights per date)
  - **Calendar Sync**: iCal import sources management (add/edit/delete/enable/disable), per-source and Sync All, iCal export feed with token generation/regeneration
- **Bookings** (`/admin/bookings`): Filterable list of all bookings by status; click through to detail
  - **Booking Detail** (`/admin/bookings/:id`): Full booking info, approve/confirm/decline/cancel actions, manual payment recording, partial/full refunds via Stripe, internal notes, payment notes, archive
- **Cleaning** (`/admin/cleaning`): Cleaning task manager - create/edit/delete tasks, assign cleaners, status tracking (needed/scheduled/in_progress/completed/skipped), filter by status
- **Maintenance** (`/admin/maintenance`): Maintenance task manager - create/edit/delete, priority (low/medium/high/urgent), status (open/in_progress/completed/archived), category (repair/cleaning/supplies/inspection/guest_reported/general), due dates, stats dashboard
- **Property** (`/admin/property`): Tabbed property editor (9 tabs - see Section 12)
- **Email** (`/admin/email`): Email settings and automation management
  - Provider configuration (SMTP/Resend), admin email, sender address, SMTP host/port/secure, credential storage via Vault
  - Provider Test, Template Test, Automation Test (sends to admin only)
  - Email templates (21 system templates, custom template support), template variable preview
  - Email automations (20 automations), trigger type configuration, recipient type, template assignment, active toggle, offset days, send time
  - Notification logs view
- **Payments** (`/admin/payments`): Payment mode selector (test_manual/test_stripe/live_manual/live_stripe), Stripe credential management (test and live secret keys, webhook secrets, publishable keys - all via Vault), site URL, checkout expiration, webhook endpoint URL display
- **Account** (`/admin/account`): Profile, owner/business info, listing info, listing manager, primary guest contact, email template property settings (address, check-in/out times, door code), website settings (logo, favicon, SEO, taglines, badges), preferences (timezone, currency, date format), system status, password reset, logout, support info
  - **Account sub-pages** (rendered as flyout/drawer links under Account in nav):
    - `/admin/account/privacy` - Platform Privacy Policy (read-only document page)
    - `/admin/account/terms` - Platform Terms & Conditions (read-only document page)
    - `/admin/account/documentation` - Platform Documentation (read-only document page)
- These sub-pages are **read-only document-style pages** backed by static content files (`src/content/legal/*.txt`), rendered via `LegalPageLayout`. They are **not editors** - the admin cannot modify privacy policy, terms, or documentation content through the UI.

---

## 6. Booking System

**Complete booking lifecycle:**

1. **Guest date selection**: Date range picker with availability checking against `public_availability` view (blocked_dates, owner_blocks, confirmed bookings, active pending_payment)
2. **Availability checks**: Validates against blocked_dates, owner_blocks, and overlapping bookings (confirmed + active pending_payment with non-expired payment_expires_at)
3. **Occupancy rules**: Max guests enforcement, adults/children/infants/pets breakdown
4. **Booking request** (manual mode): `create-booking-request` edge function creates booking with `pending_review` status, sends notification emails
5. **Booking checkout** (Stripe mode): `create-checkout-session` edge function creates Stripe Checkout session, sets booking to `pending_payment` with payment_expires_at
6. **Approval**: Admin approves pending_review booking â†’ if Stripe configured, creates checkout session for guest (pending_payment); if manual, confirms directly (blocks dates, creates cleaning task)
7. **Decline**: Admin declines pending_review booking â†’ `declined` status, notification sent
8. **Confirmation**: Via Stripe webhook (checkout.session.completed) or manual admin confirm
9. **Cancellation**: Admin cancels confirmed booking â†’ `cancelled` status, dates released (blocked_dates rows deleted), notification sent
10. **Payment expiration**: Pending_payment bookings expire after configurable timeout (default 30 min), cleaned up by cron job every 5 minutes
11. **Payment failure/conflict**: Amount mismatch or date conflict after payment â†’ `payment_conflict` status
12. **Refunds**: Full or partial via Stripe (admin-booking-action), cumulative refund tracking, `refunded` or `partially_refunded` payment status. **Refunds do NOT change `booking.status` and do NOT release dates.** Only cancellation releases dates. Refunds update `refunded_amount`, `refunded_at`, and `payment_status` only.
13. **Archiving**: Auto-archive cron at 3:30 AM daily for old bookings; manual archive from booking detail
14. **Internal notes**: `booking_internal_notes` table for admin notes per booking
15. **Payment notes**: Stored on booking record, visible in booking detail
16. **Cleaning task creation**: Auto-created on booking confirmation (checkout date), manually creatable

**Booking statuses**: `pending_review`, `pending_payment`, `confirmed`, `declined`, `expired`, `cancelled`, `payment_failed`, `payment_conflict`, `refunded`

**Payment statuses**: `paid`, `pending`, `unpaid`, `partially_paid`, `expired`, `failed`, `refunded`, `partially_refunded`

**Manual Test vs. Stripe Test vs. Manual Live vs. Stripe Live:**

| Mode | Guest Flow | Payment | Date Blocking |
| --- | --- | --- | --- |
| `test_manual`                      | Submit request â†’ admin approves â†’ confirmed                     | No online payment   | On approval (manual confirm) |
| `test_stripe`                      | Submit request â†’ Stripe Checkout (test keys) â†’ webhook confirms | Stripe test charges | On webhook confirmation      |
| `live_manual`                      | Same as test_manual                                             | No online payment   | On approval                  |
| `live_stripe`                      | Submit request â†’ Stripe Checkout (live keys) â†’ webhook confirms | Real Stripe charges | On webhook confirmation      |

---

## 7. Pricing Engine

**Nightly rate precedence (highest to lowest):**

1. **Date-specific price override** (`date_price_overrides`): Exact date match wins
2. **Seasonal pricing preset** (`seasonal_pricing_presets`): Highest priority among active presets matching date range
3. **Day-of-week rate** (`day_of_week_rates`): Matches the day of week (0=Sunday)
4. **Base nightly rate** (`properties.base_price`): Fallback

**Minimum-night precedence (highest to lowest):**

1. **Date availability override** (`date_availability_overrides.min_nights`): Exact check-in date match
2. **Seasonal pricing preset** (`seasonal_pricing_presets.min_nights`): Highest priority preset matching check-in date
3. **Property default** (`properties.min_nights`)

**Fee calculation:**

- **Cleaning fee**: A standard `per_stay` (or `per_night`) fee. It is **never** skipped based on pets. It is always included in the quote when enabled and the amount is greater than zero.
- **Pet fee**: A standard `per_stay` (or `per_night`) fee. It is skipped (amount set to 0, filtered out) **only** when the guest selects 0 pets. The skip condition is: `fee.is_standard && fee.name === 'Pets' && pets === 0`. No other fee is affected by the pet count.
- **Additional guest fee**: `per_guest_per_stay` or `per_guest_per_night`, applies after `applies_after_guests` threshold (default 3). Only charged when guest count exceeds the threshold.
- **Custom fees**: Any of `per_stay`, `per_night`, `per_guest_per_stay`, `per_guest_per_night`. Not subject to the pet-skip logic.
- **apply_to_guest_quote**: Fees with this flag set to `false` are excluded from guest-facing quotes entirely.
- **Zero-amount filtering**: All fee lines with `amount <= 0` are filtered out of the final quote.
- **Tax**: Calculated as `(subtotal + feesTotal) * tax_rate`

**Quote calculation**: Per-night sum using `resolveNightlyRate()` for each night, plus applicable fees, plus tax. Consistent between client-side (`src/lib/pricing.ts`) and server-side (`create-checkout-session`, `create-booking-request` edge functions).

---

## 8. Payments & Stripe

**Four payment modes**: `test_manual`, `test_stripe`, `live_manual`, `live_stripe`

**Credential management:**

- Separate test and live Stripe secret keys, webhook secrets, and publishable keys
- All secrets stored in Bolt Database Vault (never in env vars except as fallback)
- `payment_settings` table stores: payment_mode, site_url, checkout_expires_minutes, publishable keys (non-secret)
- Vault RPCs: `payment_settings_get_secret`, `payment_settings_secret_preview`, `payment_settings_delete_secret`

**Checkout session creation:**

- `create-checkout-session` (public, for guest-initiated checkout): Creates Stripe session, sets booking to `pending_payment`
- `create-checkout-session-for-booking` (admin/authenticated): Creates checkout session for an existing pending_review booking (used during admin approval)

**Payment expiration:**

- `payment_expires_at` set on booking at checkout creation (default 30 min, configurable)
- Cron job `cleanup-expired-pending-payments` runs every 5 minutes, expires stale pending_payment bookings

**Successful payment processing** (stripe-webhook):

1. Verifies Stripe signature (tries test secret, then live secret)
2. Enforces livemode consistency (test secret must match `livemode=false`, live must match `true`)
3. Idempotency via `payment_events` table (unique `stripe_event_id`)
4. Guards: booking must still be `pending_payment`/`pending`; amount must match; re-check date conflicts
5. On success: blocks dates, creates cleaning task, sets `confirmed`, sends notifications
6. On amount mismatch: `payment_conflict`
7. On date conflict: `payment_conflict` (payment received but dates unavailable)

**Refunds:**

- Full refund: Sets `payment_status` to `refunded`, updates `refunded_amount` and `refunded_at`. Does **not** change `booking.status`. Does **not** release dates.
- Partial refund: Updates `refunded_amount` (cumulative), sets `payment_status` to `partially_refunded`. Does **not** change `booking.status`.
- **Stripe environment selection for refunds**: The refund secret key is selected based on the **original Stripe transaction's livemode** as recorded in `payment_events`, NOT the current `payment_mode`. This ensures refunds are always processed against the same Stripe environment that received the original payment, even if the admin has since switched modes.
- Refund email automation triggered (`booking_refunded` trigger type, admin + guest)

**Stripe webhook events handled by the current implementation:**

1. `checkout.session.completed` - payment success (confirms booking, blocks dates, creates cleaning task, sends notifications)
2. `checkout.session.expired` - checkout session expired without payment (marks booking as expired)
3. `payment_intent.payment_failed` - payment failed (marks booking as `payment_failed`)
4. `charge.refunded` - refund processed externally (records event, updates refund tracking if applicable)

---

## 9. Email System

**Provider configuration:**

- SMTP (via `nodemailer@6` in edge function) or Resend (via REST API)
- Provider selected in `email_settings` table per property
- SMTP credentials (username, password) stored in Vault via `email_settings_get_secret` / `email_settings_set_secret` RPCs
- Admin email, sender address, SMTP host/port/secure stored in `email_settings` (non-secret)
- Env var fallback: `EMAIL_PROVIDER`, `SMTP_HOSTNAME`, `SMTP_PORT`, `SMTP_SECURE`, `SMTP_USERNAME`, `SMTP_PASSWORD`, `SMTP_FROM`, `FROM_EMAIL`, `ADMIN_EMAIL`, `RESEND_API_KEY`

**Three test modes:**

- **Provider Test**: Sends a simple test email to admin email, verifies provider connectivity
- **Template Test**: Renders a specific template with sample variables, sends to admin
- **Automation Test**: Sends the selected automation's assigned template to admin only, using real or sample booking data

**Templates:**

- 21 system templates (`is_system = true`), all active
- Custom templates supported (`is_system = false`)
- Template variables: 50+ variables covering property, listing, guest, booking, owner, business, and account settings
- HTML templates with `{{variable}}` interpolation, HTML-escaped (except `payment_button` which is raw HTML)
- Template CRUD performed via direct Bolt Database table operations in the frontend (`email_templates` table), not via edge functions

**Email automations (20 total):**

| Automation | Trigger | Recipient | Offset Days | Send Time |
| --- | --- | --- | ---: | --- |
| Booking Cancelled â€“ Admin                 | `booking_cancelled`        | admin | 0 | -     |
| Booking Cancelled â€“ Guest                 | `booking_cancelled`        | guest | 0 | -     |
| Booking Confirmed â€“ Admin                 | `booking_confirmed`        | admin | 0 | -     |
| Booking Confirmed â€“ Guest                 | `booking_confirmed`        | guest | 0 | -     |
| Booking Refunded â€“ Admin                  | `booking_refunded`         | admin | 0 | -     |
| Booking Refunded â€“ Guest                  | `booking_refunded`         | guest | 0 | -     |
| Booking Request Approved â€“ Admin          | `booking_request_approved` | admin | 0 | -     |
| Booking Request Approved â€“ Guest          | `booking_request_approved` | guest | 0 | -     |
| Booking Request Declined â€“ Admin          | `booking_request_declined` | admin | 0 | -     |
| Booking Request Declined â€“ Guest          | `booking_request_declined` | guest | 0 | -     |
| Booking Request Submitted â€“ Admin         | `booking_request_received` | admin | 0 | -     |
| Booking Request Submitted â€“ Guest         | `booking_request_received` | guest | 0 | -     |
| Check-In Day Reminder                     | `day_of_check_in`          | guest | 0 | 08:00 |
| Check-Out Reminder                        | `before_check_out`         | guest | 1 | 18:00 |
| In Case Of Emergency                      | `day_of_check_in`          | guest | 1 | 09:00 |
| Inquiry Auto Reply â€“ Guest                | `inquiry_received`         | guest | 0 | -     |
| New Inquiry Submitted â€“ Admin             | `inquiry_received`         | admin | 0 | -     |
| Pre-Arrival Instructions                  | `before_check_in`          | guest | 1 | 09:00 |
| Review Request                            | `after_check_out`          | guest | 1 | 10:00 |
| Stuff To Do                               | `day_of_check_in`          | guest | 1 | 12:00 |

**Notification logs**: `notification_logs` table records every send attempt (related_type, related_id, channel, provider, recipient, subject, status, template_key, error_message)

---

## 10. Calendar & iCal

**Main calendar** (`AdminCalendar.tsx`):

- Monthly grid view with navigation
- Shows: confirmed bookings, pending_review, pending_payment, payment_conflict, owner blocks, iCal imports, cleaning tasks
- Color-coded status legend and event detail popovers
- Links from booking events to booking detail page

**Owner blocks**: Date ranges with reason and block_type, shown as slate-colored events, exported in iCal

**Imported calendar activity**: `blocked_dates` rows with `source` starting with `import:`, shown as purple events with source name and platform

**Cleaning activity**: Cleaning tasks shown as yellow events on their task_date

**Availability rules**: A date is unavailable if any of: blocked_dates row exists, owner_block overlaps, confirmed booking overlaps, active pending_payment booking overlaps

**iCal export:**

- Unauthenticated endpoint (token-protected): `/functions/v1/ical-export/{property_id}/{token}.ics`
- Token stored on `properties.calendar_export_token`, generated/regenerated via `ical-export-token` edge function
- Exports only confirmed bookings (as "Reserved") and owner blocks (as "Unavailable")
- Never exposes guest names, emails, phone, booking IDs (uses SHA-256 hash UIDs)
- `Cache-Control: no-cache, no-store, must-revalidate`

**iCal import:**

- Admin-only edge function (`ical-import`) with JWT verification
- SSRF protection: blocks localhost, private IPs, metadata endpoints; 15s fetch timeout; 2MB response cap
- Per-source cleanup: deletes only `import:{source_id}` rows, then re-inserts
- Supports Airbnb, VRBO, Booking.com, Other platforms
- Manual sync (per-source or Sync All), enable/disable sources
- No automatic scheduled import (manual trigger only)

---

## 11. Photos & Media

**Homepage photo display**: `PropertyHero` renders a 5-photo grid (1 large hero + 4 small grid images) at 480px height. Clicking any photo opens the shared `Lightbox` component (`src/components/photos/Lightbox.tsx`). The `PhotoGallery.tsx` component exists in source but is **dead code** - not imported by any active page.

**Shared Lightbox** (`src/components/photos/Lightbox.tsx`):

- Full-screen overlay with prev/next navigation, keyboard support, close button
- Image rendered with `max-h-[583px] max-w-[1036px] object-contain` - no aspect-ratio enforcement; images preserve their natural aspect ratio within these bounds
- No thumbnail strip (unlike the unused `PhotoGallery.tsx` which has one)

**Photo Tour** (`PhotoTourPage.tsx`):

- `photo_sections` table: section title, slug, sort order, features, amenities
- `photo_section_features` table: feature/amenity callouts per section
- Section `id` used for DOM navigation is `sec.slug` (not `sec.id`)
- Section titles displayed as tab navigation
- IntersectionObserver highlights active section tab
- Hash deep-linking: `/photos#section-slug` navigates to specific section
- Shared `Lightbox` with global photo indexing across all sections

**Homepage "Where You'll Sleep"**: `sleeping_arrangements` table synced with Photo Tour - lookup keyed by `sec.slug`, matching slugs like `section-bedroom-1`. Falls back to `sleeping_arrangements.room_name` and `sleeping_arrangements.image_url` when no matching Photo Tour section is found.

**Photo upload optimization** (in `PhotosEditor.tsx`):

- **Source-size limit**: 25MB (`MAX_SIZE = 25 * 1024 * 1024`)
- **Allowed types**: JPEG, PNG, WebP
- **Max dimension**: 3000px on longest side (`MAX_DIMENSION = 3000`)
- **No upscaling**: Images smaller than 3000px are never enlarged; scale factor remains 1
- **JPEG quality**: 0.85
- **WebP quality**: 0.85
- **PNG handling**: PNG is preserved as PNG (no quality parameter, lossless)
- **Orientation**: EXIF orientation respected via `createImageBitmap(file, { imageOrientation: 'from-image' })`
- **Sequential uploads**: Files processed sequentially in a `for...of` loop (not parallel)
- **Cleanup**: `bitmap.close()` and `URL.revokeObjectURL()` in `finally` block to prevent memory leaks
- **Cover photo**: First photo in each section (sort_order) is labeled "Cover" in the editor UI

**Storage buckets**: `property-photos` (property photos), `branding` (logo, favicon, host photo)

---

## 12. Property Content Management

**Property Editor** (`PropertyEditor.tsx`) - 9 tabs:

1. **Basic Info**: Title, description, host info (name, photo, years hosting, response rate, bio), bedrooms/beds/bathrooms, max guests, base price, tax rate, min/max nights, min notice days, max advance days, deposit percentage, display price mode
2. **Highlights**: 3 feature callouts with icon, heading, subtitle, sort order
3. **Amenities**: Toggle amenities on/off, add custom amenities with icon, delete amenities
4. **Neighborhood**: Neighborhood highlights with subtitle
5. **Contact**: Contact info (phone, email, response time)
6. **Photos**: Photo section management (create/reorder sections, upload photos with client-side optimization, set cover/first photo, section features/amenities)
7. **Pricing**: Sub-tabs for **Rates** (base price, day-of-week rates, date price overrides, seasonal pricing presets with priority, min-nights, active toggle) and **Fees** (cleaning, pet, additional guest, custom fees with type, amount, threshold, apply-to-guest-quote toggle)
8. **Things to Know** (internal key `policies`): Homepage visibility toggle, custom section heading, house rules title/description, cancellation policy title/description, safety & property title/description (each description capped at 200 characters)
9. **Sections & Content** (internal key `sections`): Section visibility flags (Local Recommendations, FAQs, Guest Information) + embedded `ContentEditor` with 4 sub-tabs:
   - **FAQs**: Q&A pairs with category, visibility toggle, sort ordering
   - **House Rules**: Rule entries with icon, title, description, visibility toggle, sort ordering
   - **Policies**: Cancellation, check-in/out, pet, accessibility policy entries with title, content, visibility toggle
   - **Local Recs**: Nearby recommendations with name, category, description, address, distance label, website URL, featured flag, visibility toggle, sort ordering

---

## 13. Cleaning & Maintenance

**Cleaning:**

- Automatic cleaning tasks created on booking confirmation (task_date = checkout_date, status = "needed")
- Manual task creation supported
- Statuses: `needed`, `scheduled`, `in_progress`, `completed`, `skipped`
- Cleaner assignment via `assigned_to` field
- Notes field
- Filter by status
- Mark complete with timestamp
- Delete tasks

**Maintenance:**

- Manually created maintenance tasks
- Priorities: `low`, `medium`, `high`, `urgent`
- Statuses: `open`, `in_progress`, `completed`, `archived`
- Categories: `repair`, `cleaning`, `supplies`, `inspection`, `guest_reported`, `general`
- Due dates (optional)
- Stats dashboard (open count, urgent count, total)
- Filter by status (active/all/open/in_progress/completed/archived)
- Mark complete with timestamp
- Delete tasks

---

## 14. Account, Branding & Website Settings

- **Administrator profile**: Full name, phone, login email, created date
- **Owner/business information**: Owner name, email, phone; business name, address
- **Listing information**: Listing name, address, city, state, zip, country; listing manager name, role, email, phone; primary guest contact name, email, phone
- **Email template property settings**: Property address, check-in time, check-out time, suggested door code (used as email template variables)
- **Preferences**: Timezone (14 options), currency (8 options), date format (3 options)
- **Website settings**: Logo URL (uploaded to `branding` bucket), favicon URL, SEO title, meta description, homepage taglines (array), tagline accent color, badges (array), show/hide tagline+badges toggle
- **System status**: Payment mode, email provider, calendar source count, account ID
- **Password reset**: Triggers Bolt Database Auth password reset email
- **Logout**: Clears session
- **Support information**: Support email, phone, message, hours, enabled toggle (read-only - developer-controlled)
- **Admin sub-pages** (under Account in nav): Privacy Policy, Terms & Conditions, Documentation - all read-only document-style pages backed by static content files, rendered via `LegalPageLayout`. Not editable through the admin UI.

---

## 15. Authentication, Authorization & Security

**Authentication provider**: Bolt Database Auth (email/password)

**Login**: `/admin/login` - email/password, creates JWT session
**Password reset**: `/admin/reset-password` - Bolt Database Auth reset flow
**Public registration**: Not supported - admin accounts are pre-provisioned

**Admin authorization - verified RLS behavior:**

- `admin_users` table: RLS enabled. The **only** policy is `select_own_admin_users` (SELECT where `auth.uid() = user_id`). There are **no INSERT, UPDATE, or DELETE policies** on this table. This means:
  - Authenticated users can SELECT only their own row
  - No user (including authenticated admins) can insert, update, or delete admin users through the client API
  - New admin users must be provisioned via SQL/migration directly - this is a deliberate security hardening measure
- `account_settings` table: RLS enabled. Anon can SELECT public branding columns only (`anon_select_public_branding`). Authenticated users have full CRUD gated by `is_admin_user()` check on all write operations and SELECT.
- Most public content tables (properties, amenities, highlights, reviews, FAQs, etc.): anon SELECT for public display; administrative writes are governed by the table's RLS policies and the `admin_users` allowlist where `is_admin_user()` is used.
- `bookings`: no direct public access; booking creation happens through edge functions with server-side validation, while admin-side access is protected by authentication/RLS and the project's admin authorization model.
- `blocked_dates`: authenticated SELECT only (public reads via `public_availability` view)
- `payment_settings`, `email_settings`: admin-facing configuration is protected by authentication/RLS; secret values are kept in Vault and accessed through controlled RPC/Edge Function paths rather than normal table columns.
- `admin_users` allowlist enforced via `is_admin_user()` database function, called by RLS policies on protected tables

**Edge Function authorization:**

- Public (no JWT): `create-booking-request`, `create-checkout-session`, `stripe-webhook`, `payment-config-public`, `booking-lookup`, `ical-export`, `send-notifications`, `send-automated-emails`
- JWT required: `admin-booking-action`, `email-settings-status`, `email-settings-update`, `payment-settings-status`, `payment-settings-update`, `create-checkout-session-for-booking`, `ical-export-token`, `ical-import`, `email-templates-update`, `email-templates-reset`
- `send-automated-emails`: Accepts both service-role key (cron) and user JWT (admin test)

**Security-hardening items (implemented, verified):**

- `admin_users` table has no client-write policies - admin provisioning is SQL-only
- `account_settings` anon access restricted to branding columns only
- `public_availability` view secured with SELECT-only grants
- Stripe webhook signature verification with livemode consistency check
- iCal import SSRF protection (private IP/localhost/metadata blocking)
- iCal export token-gated with no PII in feed
- Payment amount verification on webhook
- Date conflict re-checking on webhook and admin actions
- Idempotency via `payment_events` unique constraint
- Refund Stripe environment selected from original transaction livemode, not current mode

---

## 16. Database Inventory

**Property application tables (\~40):**

`properties`, `account_settings`, `admin_users`, `users` (legacy, 0 rows), `bookings`, `booking_internal_notes`, `blocked_dates`, `owner_blocks`, `date_availability_overrides`, `date_price_overrides`, `day_of_week_rates`, `seasonal_pricing_presets`, `property_fees`, `inquiries`, `cleaning_tasks`, `maintenance_notes`, `highlights`, `amenities`, `amenity_categories`, `property_amenities`, `neighborhood_highlights`, `local_recommendations`, `faqs`, `house_rules`, `property_policies`, `sleeping_arrangements`, `reviews`, `contact_info`, `property_images`, `photo_sections`, `photo_section_features`, `ical_sources`, `notification_logs`, `payment_events`, `payment_settings`, `email_settings`, `email_templates`, `email_automations`, `email_automation_sends`

**Platform SaaS tables (30+ - NOT part of a fresh client deployment):**

`platform_profiles`, `platform_clients`, `platform_instances`, `platform_settings`, `platform_client_handoffs`, `platform_client_subscriptions`, `platform_client_lifecycle`, `platform_client_lifecycle_events`, `platform_deployment_blueprints`, `platform_deployment_blueprint_steps`, `platform_generated_setup_tasks`, `platform_instance_domains`, `platform_instance_dns_records`, `platform_instance_env_requirements`, `platform_instance_health_checks`, `platform_instance_launch_packages`, `platform_provisioning_jobs`, `platform_provisioning_job_events`, `platform_provisioning_steps`, `platform_provider_integrations`, `platform_qa_checks`, `platform_stripe_webhook_events`, `platform_support_tickets`, `platform_support_ticket_events`, `platform_support_access_logs`, `platform_template_versions`, `platform_update_jobs`, `platform_update_job_targets`, `platform_alerts`, `platform_alert_events`, `client_config`

**Views:**

- `public_availability` - Public-facing availability view (dates, availability status, reasons)

**Database functions/RPCs:**

**Property application:**

- `auto_archive_old_bookings()` - Archives old bookings
- `cleanup_expired_pending_payment_bookings()` - Expires stale pending_payment bookings
- `cleanup_old_inquiries()` - Deletes old inquiries
- `is_admin_user()` - Checks admin_users allowlist
- `payment_settings_get_secret(p_name)` - Retrieves secret from Vault
- `payment_settings_secret_preview(p_name)` - Returns masked secret preview
- `payment_settings_delete_secret(p_name)` - Deletes secret from Vault
- `email_settings_get_secret(p_name)` - Retrieves email secret from Vault
- `email_settings_set_secret(p_name, p_value)` - Stores email secret in Vault
- `update_email_settings_updated_at()` - Trigger function
- `update_email_templates_updated_at()` - Trigger function
- `update_payment_settings_updated_at()` - Trigger function
- `run_automated_emails()` - Scheduled email scheduler (called by pg_cron)

**Platform SaaS (NOT part of fresh client deployment):**

- `is_platform_super_admin()`, `create_default_client_handoff()`, `create_default_client_subscription()`, `create_default_env_requirements()`, `create_default_lifecycle()`, `create_default_provisioning_steps()`, `default_launch_checklist()`, `seed_instance_health_checks()`, `sync_instance_billing_status()`, `sync_instance_provisioning_status()`, `trg_create_launch_package_fn()`, `trg_seed_health_checks_fn()`, `update_platform_updated_at()`

---

## 17. Edge Functions

### Canonical fresh-client property Edge Functions (16)

These are the Edge Functions that belong to the canonical V1 fresh-client deployment package.

| # | Function | Auth / Gate | Purpose |
| ---: | --- | --- | --- |
| 1 | `create-booking-request` | Public endpoint with server-side validation | Creates a `pending_review` booking, validates availability/pricing, sends notifications |
| 2 | `booking-lookup` | Public lookup flow | Looks up booking information for success/cancel pages |
| 3 | `payment-config-public` | Public | Returns active payment mode and publishable key only; no secrets |
| 4 | `send-notifications` | Internal/service workflow | Sends event-based booking/inquiry notifications |
| 5 | `send-automated-emails` | Service-role cron / authenticated admin test path | Runs scheduled email automations and manual Automation Test |
| 6 | `create-checkout-session` | Public guest flow | Creates Stripe Checkout for guest-initiated bookings |
| 7 | `stripe-webhook` | Stripe signature verification | Handles `checkout.session.completed`, `checkout.session.expired`, `payment_intent.payment_failed`, `charge.refunded` |
| 8 | `ical-export` | Token-protected public feed | Dynamically generates iCal feed for confirmed bookings and owner blocks |
| 9 | `admin-booking-action` | Admin/JWT + allowlist checks | Approve, confirm, decline, cancel, mark paid, full/partial refund |
| 10 | `email-settings-status` | Admin/JWT | Returns email-provider configuration status |
| 11 | `email-settings-update` | Admin/JWT | Updates email configuration and protected credentials |
| 12 | `payment-settings-status` | Admin/JWT | Returns payment configuration status and masked credential state |
| 13 | `payment-settings-update` | Admin/JWT | Updates payment mode/settings and protected credentials |
| 14 | `create-checkout-session-for-booking` | Admin/JWT | Creates Stripe Checkout link for an existing booking after admin approval |
| 15 | `ical-export-token` | Admin/JWT | Generates/regenerates the iCal export token |
| 16 | `ical-import` | Admin/JWT | Imports external iCal feeds into blocked dates |

### Extra master-live noncanonical property functions (2)

These functions are currently deployed in the master live environment but are **not part of the canonical fresh-client V1 deployment** and are not used by the current frontend template-management flow.

| Function | Status |
| --- | --- |
| `email-templates-update` | Master-live only; JWT protected; current frontend performs template updates directly against the database under RLS |
| `email-templates-reset` | Master-live only; JWT protected; current frontend does not call it |

### Platform-only Edge Functions (14)

These are master/platform provisioning functions and are **not copied to a fresh property client**:

`platform-bootstrap-client-database`, `platform-create-netlify-site`, `platform-connect-netlify-source`, `platform-connect-netlify-domain`, `platform-set-netlify-env-vars`, `platform-refresh-netlify-site-info`, `platform-netlify-diagnostic`, `platform-duplicate-template-source`, `platform-setup-Bolt Database-instance`, `platform-run-instance-health-checks`, `platform-sync-stripe-subscription`, `platform-stripe-subscription-webhook`, `platform-generate-alerts`, `platform-test-provider-integration`.

**Master-live deployed total:** 32 = 16 canonical client + 2 extra master-live noncanonical + 14 platform-only.

---


## 18. Scheduled / Automated Backend Jobs

All jobs use `pg_cron` with `pg_net` for HTTP calls. Schedules are defined in migration files. **Live cron state could not be verified** because the `cron.jobs` system table is not queryable via the available SQL tool. The following schedules are confirmed from migration source only:

| Job Name | Schedule (from migrations) | Purpose | Mechanism |
| --- | --- | --- | --- |
| `cleanup-expired-pending-payments`                 | Every 5 min   | Expires stale pending_payment bookings past their payment_expires_at | `SELECT public.cleanup_expired_pending_payment_bookings()`                                      |
| `auto-archive-old-bookings`                        | 3:30 AM daily | Archives old completed/cancelled bookings                            | `SELECT auto_archive_old_bookings()`                                                            |
| `cleanup-old-inquiries`                            | 3:00 AM daily | Deletes old inquiries past retention                                 | `SELECT cleanup_old_inquiries()`                                                                |
| `run-automated-emails`                             | Every 15 min  | Evaluates and sends scheduled/date-based email automations           | `SELECT public.run_automated_emails()` (calls `send-automated-emails` edge function via pg_net) |

**Timezone considerations**: Cron jobs run in the database server's timezone; email automations use the property's configured timezone from `account_settings` for offset/send-time calculations.

---

## 19. Storage

| Bucket | Purpose | Access |
| --- | --- | --- |
| `branding`          | Logo, favicon, and host photo uploads         | Authenticated upload, public read |
| `property-photos`   | Property listing photos and Photo Tour images | Authenticated upload, public read |

---

## 20. Environment Variables & External Configuration

**Frontend (VITE\_ prefix, exposed to browser):**

- `VITE_SUPABASE_URL`, `VITE_SUPABASE_ANON_KEY`, `VITE_BOOKING_PAYMENT_MODE`

**Database/Edge Function (server-side, auto-set by Bolt Database):**

- `SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY`, `SUPABASE_ANON_KEY`

**Stripe (stored in Vault, env fallback):**

- `STRIPE_SECRET_KEY`, `STRIPE_WEBHOOK_SECRET`, `SITE_URL`, `STRIPE_CHECKOUT_EXPIRES_MINUTES`

**Email (stored in Vault/email_settings, env fallback):**

- `EMAIL_PROVIDER`, `SMTP_HOSTNAME`, `SMTP_PORT`, `SMTP_SECURE`, `SMTP_USERNAME`, `SMTP_PASSWORD`, `SMTP_FROM`, `FROM_EMAIL`, `ADMIN_EMAIL`, `RESEND_API_KEY`

**Other (referenced but not actively configured):**

- `TWILIO_ACCOUNT_SID`, `TWILIO_AUTH_TOKEN`, `TWILIO_FROM_NUMBER`, `ADMIN_PHONE`

---

## 21. Routes

**Public routes:**

- `/` - Property listing page
- `/property/:id` - Property listing page (alt)
- `/photos` - Photo Tour page
- `/booking/success` - Stripe checkout success
- `/booking-success` - Alternative booking success page
- `/booking/cancelled` - Stripe checkout cancelled
- `/booking/request-success` - Manual booking request confirmation
- `/inquiry/success` - Inquiry submission confirmation
- `/privacy` - Guest privacy policy
- `/terms` - Guest terms & conditions

**Admin routes:**

- `/admin/login` - Admin login
- `/admin/reset-password` - Password reset
- `/admin` - Admin dashboard (Overview)
- `/admin/calendar` - Calendar (with Availability and Calendar Sync sub-routes)
- `/admin/calendar-sync` - Calendar Sync (direct)
- `/admin/bookings` - Bookings list
- `/admin/bookings/:bookingId` - Booking detail
- `/admin/cleaning` - Cleaning manager
- `/admin/maintenance` - Maintenance manager
- `/admin/property` - Property editor (9 tabs)
- `/admin/email` - Email settings and automations
- `/admin/payments` - Payment settings
- `/admin/account` - Account and website settings
- `/admin/account/privacy` - Platform Privacy Policy (read-only document)
- `/admin/account/terms` - Platform Terms & Conditions (read-only document)
- `/admin/account/documentation` - Platform Documentation (read-only document)

**Platform SaaS routes:**

- `/platform/*` - Platform management dashboard

---

## 22. External Services / Integrations

- **Stripe** - Payment processing (Checkout Sessions, refunds, webhooks); test and live credentials
- **SMTP server** - Email delivery via configurable SMTP
- **Resend** - Email delivery via Resend API (alternative to SMTP)
- **External iCal platforms** - Airbnb, VRBO, Booking.com (import feeds, export feed)
- **Bolt Database** - Database, Auth, Storage, Edge Functions, Vault
- **Netlify** - Frontend hosting + platform SaaS client provisioning
- **Bolt Database Management API** - Platform SaaS client instance provisioning
- **Twilio** - SMS notifications (referenced in code, not actively configured)

---

## 23. Complete V1 Guest Flow

1. **Guest lands on site** (`/`) â†’ sees property listing with photos, description, amenities, host info, reviews, FAQs
2. **Explores listing** â†’ scrolls through sections, opens Photo Tour (`/photos`), views amenities, neighborhood, Things to Know
3. **Selects dates and guests** in booking card â†’ date range picker with real-time availability check, guest count selector (adults/children/infants/pets)
4. **Sees pricing quote** â†’ nightly rate breakdown (with day-of-week/seasonal pricing), fees, taxes, total - calculated client-side, mirrored server-side
5. **Booking path branches:**
   - **Manual mode** (`test_manual`/`live_manual`): Clicks "Request to Book" â†’ `create-booking-request` edge function â†’ `pending_review` status â†’ admin notification email â†’ guest confirmation email â†’ guest sees `/booking/request-success`
   - **Stripe mode** (`test_stripe`/`live_stripe`): Clicks "Checkout" â†’ `create-checkout-session` edge function â†’ Stripe Checkout redirect â†’ payment â†’ `pending_payment` status â†’ Stripe webhook â†’ `confirmed` status â†’ date blocking, cleaning task creation, confirmation emails â†’ guest sees `/booking/success`
6. **Confirmation** â†’ booking confirmed, dates blocked, cleaning task auto-created, confirmation emails sent to guest and admin
7. **Calendar** â†’ confirmed booking appears on admin calendar, exported in iCal feed
8. **Cleaning** â†’ cleaning task appears on cleaning schedule for checkout date

---

## 24. Complete V1 Administrator Flow

1. **Setup** â†’ Login at `/admin/login` â†’ configure property content (basic info, highlights, amenities, photos, pricing, fees, policies) â†’ configure payment settings (Stripe credentials, mode) â†’ configure email settings (SMTP/Resend, admin email) â†’ configure calendar sync (iCal sources, export token) â†’ configure account settings (branding, SEO, timezone)
2. **Launch** â†’ Property is live, guests can book
3. **Booking management** â†’ Monitor calendar for new bookings â†’ review pending_review bookings â†’ approve (sends payment link or confirms manually) or decline â†’ manage confirmed bookings (view detail, add notes, record manual payments, process refunds)
4. **Payment/refund** â†’ Monitor payment status â†’ record manual payments (cash/Zelle/Venmo/PayPal/other) â†’ process full/partial refunds via Stripe â†’ track refund amounts
5. **Email** â†’ Configure email automations â†’ test provider/template/automation â†’ monitor notification logs â†’ customize email templates
6. **Calendar** â†’ Monitor calendar â†’ manage owner blocks â†’ sync iCal sources â†’ manage availability overrides â†’ share export feed
7. **Cleaning/maintenance** â†’ Monitor cleaning schedule â†’ assign cleaners â†’ mark complete â†’ create/track maintenance tasks
8. **Content management** â†’ Update property content â†’ manage photos and Photo Tour â†’ update pricing/seasonal presets â†’ manage FAQs/reviews/recommendations â†’ update branding/SEO

---

## 25. What V1.0.0 Does NOT Do

- **Multi-property management**: Hard-coded to a single property ID
- **Public self-registration**: No public user registration; admin accounts pre-provisioned via SQL
- **Real-time booking modifications by guests**: Guests cannot modify or cancel their own bookings
- **Automated iCal import scheduling**: Manual trigger only
- **SMS notifications**: Twilio referenced but not configured
- **Dynamic pricing algorithms**: All pricing manually configured
- **Guest accounts/portals**: No guest-facing account pages
- **Multi-currency payment processing**: Currency configurable but Stripe charges always in USD
- **Automated refunds**: Refunds always admin-initiated
- **Review responses**: Admin cannot respond to reviews
- **Multi-language support**: No i18n
- **Progressive Web App**: No service worker or offline support
- **Analytics/tracking**: No built-in analytics

---

## 26. Known Technical / Security Follow-Up Items

### Harmless technical debt

- Legacy `users` table exists with RLS enabled but 0 rows; the active admin model uses Bolt Database Auth plus `admin_users`.
- Legacy Express/Prisma backend exists in `/backend` but is not part of the active property deployment.
- `PhotoGallery.tsx` remains in source as dead code; the active homepage flow is `PropertyHero` + shared `Lightbox`.
- `email-templates-update` and `email-templates-reset` remain deployed in the master live environment but are noncanonical and are not part of fresh-client V1 deployment.
- `client_prelaunch_cleanup.sql` and `ultimate_client_database_bootstrap.sql` are standalone operational SQL files rather than normal migration files.
- Some migration filenames contain a double `.sql.sql` suffix; this is cosmetic.

### Security hardening already implemented

- `admin_users` allowlist is enforced and client-side admin-user provisioning is not available.
- Anonymous access to `account_settings` is restricted to intended public branding data.
- `public_availability` is exposed as a controlled public availability surface rather than direct public booking-table access.
- Stripe webhook signatures and live/test consistency are verified.
- iCal import includes SSRF protections against private/local/metadata targets.
- iCal export is token-protected and excludes guest PII.
- Stripe payment amounts and date conflicts are re-checked server-side.
- `payment_events` provides webhook idempotency.
- Refunds select the Stripe environment from the original transaction's recorded `livemode`, not the currently selected payment mode.

### Carry-forward security-hardening checklist

The following items were identified in prior security review as hardening/cleanup work. They were not all re-verified during the final targeted V1 inventory pass, so they are preserved here rather than silently dropped:

- `property_amenities` UPDATE-policy coverage - verify/complete if still outstanding.
- Payment secret-preview RPC permissions - keep restricted to appropriate authenticated/admin callers.
- Maintenance `SECURITY DEFINER` function execute grants - verify anon/auth execution is no broader than necessary.
- Database function `search_path` hardening - continue normalizing security-sensitive functions where needed.
- Unnecessary anon/auth grants on protected tables such as `admin_users` - remove where redundant even if RLS currently blocks effective access.

These are **hardening items**, not verified active exploits or broken V1 functionality.

### Functional limitations by design

- iCal import is manually triggered; there is no automatic periodic import scheduler.
- Guests do not have self-service accounts, booking modification, or cancellation.
- V1 is single-property, not a marketplace or multi-property manager.
- SMS/Twilio references exist but SMS delivery is not configured as an active V1 capability.

**Known active bugs:** No verified active bug remained at the time this V1.0.0 baseline was finalized.

---

## 27. V1.0.0 Feature Checklist

- Public property listing page with full content
- Photo Tour with sections, lightbox, deep-linking
- Sleeping arrangements synced with Photo Tour
- Amenities with categories and custom items
- Neighborhood highlights and local recommendations
- Reviews display
- FAQs with visibility controls
- Things to Know (house rules, policies, check-in/out, pet policy, accessibility)
- Contact/inquiry form with email notifications
- Date range picker with real-time availability
- Guest count selector (adults/children/infants/pets)
- Client-side pricing quote (4-level nightly rate, fees, taxes)
- Minimum-night enforcement (3-level precedence)
- Booking request flow (manual mode)
- Stripe Checkout flow (test and live)
- Booking lifecycle (pending_review â†’ pending_payment â†’ confirmed â†’ cancelled), with refund state tracked separately in payment/refund fields
- Payment expiration with cron cleanup
- Admin booking actions (approve, confirm, decline, cancel, mark_paid, refund)
- Full and partial refunds via Stripe
- Cumulative refund tracking
- Manual payment recording (cash/Zelle/Venmo/PayPal/other)
- Internal notes and payment notes per booking
- Booking auto-archiving
- Admin calendar with all event types
- Availability editor (owner blocks, date overrides)
- iCal import (per-source and Sync All, SSRF protection)
- iCal export (token-protected, no PII)
- iCal export token generation/regeneration
- Pricing editor (base, day-of-week, date overrides, seasonal presets)
- Fees editor (cleaning, pet, additional guest, custom, apply-to-guest-quote)
- Property content editor (all sections)
- Photos editor with sections and features
- Email system (SMTP + Resend)
- 21 email templates (system + custom)
- 20 email automations (event-based + scheduled)
- Automated email scheduler (pg_cron every 15 min)
- Email provider test, template test, automation test
- Automation test with in-memory sample booking fallback
- Notification logging
- Cleaning task management (auto + manual)
- Maintenance task management
- Account/branding settings (logo, favicon, SEO, taglines, badges)
- Website branding applied dynamically
- Admin authentication with allowlist
- Stripe credential management (test + live, Vault)
- Email credential management (Vault)
- RLS on all tables
- Public availability view
- Storage buckets (branding, property-photos)
- Responsive/mobile design
- Public legal pages (privacy, terms)
- Platform SaaS management layer documented separately from the canonical fresh-client property application

---

## 28. Inspection Coverage / Self-Audit

| Source | Status |
| --- | --- |
| Frontend routes/pages         | Reviewed (App.tsx, all page components)                                                                                                                                            |
| Major components              | Reviewed (BookingCard, AdminCalendar, BookingDetail, PropertyEditor, PaymentsEditor, AccountPage, CleaningManager, MaintenanceManager, CalendarSync, EmailSettings, PhotoTourPage) |
| Database/bootstrap/migrations | Reviewed (list_tables, list_migrations, key migration files via Grep)                                                                                                              |
| RLS                           | Reviewed (security posture via migrations, account_settings anon restriction, admin_users allowlist)                                                                               |
| Edge Functions                | Reviewed (16 canonical fresh-client property functions + 2 extra master-live noncanonical functions + 14 platform-only functions; key functions read in full)                                                                                    |
| Scheduled jobs                | Reviewed (pg_cron schedules via migration Grep: 4 jobs confirmed)                                                                                                                  |
| Storage                       | Reviewed (2 buckets: branding, property-photos)                                                                                                                                    |
| Environment references        | Reviewed (.env.example, config.toml, edge function env usage)                                                                                                                      |
| Package/config files          | Reviewed (package.json, netlify.toml, supabase/config.toml, tsconfig)                                                                                                              |
| Payment flow                  | Reviewed (create-checkout-session, create-checkout-session-for-booking, stripe-webhook, admin-booking-action, payment-config-public, payment-settings-status/update)               |
| Email flow                    | Reviewed (send-notifications, send-automated-emails, email-settings-status/update, email automations table, email templates table)                                                 |
| Calendar flow                 | Reviewed (ical-import, ical-export, ical-export-token, CalendarSync component, AdminCalendar component)                                                                            |
| Pricing logic                 | Reviewed (src/lib/pricing.ts, create-booking-request, create-checkout-session edge function pricing)                                                                               |
| Database functions/RPCs       | Reviewed (20 functions via information_schema.routines)                                                                                                                            |
| Database views                | Reviewed (1 view: public_availability)                                                                                                                                             |
| Database triggers             | Unable to Verify (information_schema.triggers returned empty; trigger functions exist but trigger bindings not surfaced)                                                           |
| pg_cron active jobs           | Unable to Verify (cron.jobs table not accessible via SQL; schedules confirmed via migration files)                                                                                 |

**Unable to verify items:**

- Active pg_cron job status (the `cron.jobs` system table is not queryable via the MCP SQL tool; job schedules are confirmed from migration files but current active/inactive state could not be verified)
- Database trigger bindings (the `information_schema.triggers` view returned empty despite trigger functions existing; triggers may be defined via `pg_trigger` catalog directly)

## 29. Final Verification Corrections

1. **Edge Function classification - Corrected.** Canonical fresh-client V1 contains **16** property Edge Functions. The master live environment also has **2** noncanonical property functions (`email-templates-update`, `email-templates-reset`) that are not copied to fresh clients. The platform layer has **14** platform-only Edge Functions. Master-live deployed total: **32**.
2. **Refund behavior - Corrected.** Full/partial refunds do **not** change `booking.status` and do **not** release dates. Refunds update refund/payment fields; cancellation is the operation that releases dates.
3. **Refund Stripe environment selection - Corrected.** Refund credentials are selected from the original Stripe transaction/event `livemode` recorded in `payment_events`, not the current `payment_mode`.
4. **Stripe webhook events - Corrected.** Current implementation handles `checkout.session.completed`, `checkout.session.expired`, `payment_intent.payment_failed`, and `charge.refunded`.
5. **Active homepage/photo implementation - Corrected.** `PropertyHero` + shared `Lightbox` is active. `PhotoGallery.tsx` is dead code.
6. **Photo upload optimization - Corrected.** 25MB source limit, 3000px maximum longest edge, no upscaling, JPEG/WebP quality 0.85, PNG preserved losslessly, EXIF-aware orientation path, sequential processing, cleanup of image resources.
7. **Shared lightbox sizing - Corrected.** Active image uses `max-h-[583px] max-w-[1036px] w-auto h-auto object-contain`, preserving natural aspect ratio within those bounds.
8. **Admin legal/documentation routes - Corrected.** `/admin/account/privacy`, `/admin/account/terms`, `/admin/account/documentation`.
9. **Admin Privacy/Terms/Documentation page type - Corrected.** These are read-only document pages backed by static content, not UI editors.
10. **Property admin structure - Corrected.** Nine tabs: Basic Info, Highlights, Amenities, Neighborhood, Contact, Photos, Pricing, Things to Know, Sections & Content; Sections & Content contains FAQs, House Rules, Policies, Local Recs.
11. **Fee logic - Corrected.** Cleaning fee is not pet-dependent. Only the standard Pets fee is skipped when pet count is zero.
12. **RLS/admin allowlist wording - Corrected.** Admin access is governed by authentication/RLS and the allowlist model; the report avoids equating every authenticated user with an administrator.
13. **Scheduled jobs live state - Unable to verify.** Four schedules are confirmed from migration/source definitions; live `cron.jobs` active state was not queryable during the audit.
14. **Canonical client vs. platform separation - Corrected.** Canonical client deployment = 16 Edge Functions and property application database objects. Two extra property functions are master-live only. Platform SaaS infrastructure is separate and not part of fresh-client deployment.
15. **Where You'll Sleep - Corrected.** Photo Tour lookup uses section slug matching, pulls the current Photo Tour title and first ordered photo, deep-links to the matching section, and falls back to sleeping-arrangement title/image when no match exists.

---

## V1.0.0 Baseline Status

- **Release baseline:** Finalized
- **Canonical fresh-client Edge Functions:** 16
- **Extra master-live noncanonical property functions:** 2 (`email-templates-update`, `email-templates-reset`)
- **Platform-only Edge Functions:** 14
- **Master-live deployed Edge Function total:** 32
- **Verified active bugs at baseline:** None identified
- **Unable to verify live state:** Active `pg_cron` job state; database trigger bindings
- **Report completeness:** All 29 required sections are present
- **Use going forward:** Treat this document as the V1.0.0 architecture/feature baseline. Record later bug fixes and incremental changes in the changelog; revise this baseline only when a material system capability or architecture changes.
